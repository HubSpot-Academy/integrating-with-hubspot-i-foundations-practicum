doctype html
html
    head
        title New CRM Record
        Link(rel="stylesheet", href="/css/style.css")
        body
            .header
                h1 Create New CRM Record
                a(href='/') Return to the homepage
            form(method='POST', action='/update-cobj')
                .form-group
                    label(for='name') Name * : 
                    input#name(type='text', name='pet_name', required=true)

                .form-group
                    label(for='breed') Breed * :
                    input#breed(type='text', name='breed', required=true)

                .form-group
                    label(for='age') Age * :
                    input#age(type='number', name='age', required=true)

                .form-group
                    label(for='type') Type :
                    select#type(name='type', required=true)
                        option(value='Dog') Dog
                        option(value='Cat') Cat
                        option(value='Rabbit') Rabbit
                        option(value='Snake') Snake

                button(type='submit') Submit
