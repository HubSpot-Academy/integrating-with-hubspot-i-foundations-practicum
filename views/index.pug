doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Index
        h2 Custom Object Exists
        .cards 
            each custom in data
                .card 
                    h2.card__name test
                    p.card__email #{custom}